{
    "id": "ssh-genovia-sdma",
    "name": "SSH Genovia SecureDrop Admin",
    "type": "ssh",
    "group": "{TEAM}",
    "score_weight": 1,
    "definition": {
        "Host": "{{.Host}}",
        "Requests": [
            {
                "Username": "{{.Username}}"
            }
        ],
        "CaIP": "{{.CaIP}}",
        "SSHCertificateAuthorityPassword": "{{.SSHCertificateAuthorityPassword}}",
        "Cmd": "{{.Cmd}}",
        "ContentRegex": "{{.ContentRegex}}",
        "MatchContent": "{{.MatchContent}}"
    }
}