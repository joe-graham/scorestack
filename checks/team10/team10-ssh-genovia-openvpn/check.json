{
    "id": "ssh-genovia-openvpn",
    "name": "SSH Genovia OpenVPN",
    "type": "ssh",
    "group": "team10",
    "score_weight": 1,
    "definition": {
        "Host": "{{.Host}}",
        "Requests": [
            {
                "Username": "{{.Username}}"
            }
        ],
        "CaIP": "{{.CaIP}}",
        "SSHCertificateAuthorityPassword": "{{.SSHCertificateAuthorityPassword}}",
        "Cmd": "{{.Cmd}}",
        "ContentRegex": "{{.ContentRegex}}",
        "MatchContent": "{{.MatchContent}}"
    }
}